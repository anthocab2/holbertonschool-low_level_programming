#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    printf("%d\n", _pow_recursion(2, 5));
    printf("%d\n", _pow_recursion(2, 0));
    printf("%d\n", _pow_recursion(2, -1));
    return (0);
}
