#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    printf("%d\n", _sqrt_recursion(25));
    printf("%d\n", _sqrt_recursion(16));
    printf("%d\n", _sqrt_recursion(17));
    return (0);
}
