#include "main.h"

/**
 * print_numbers - prints numbers from 0 to 9
 */
void print_numbers(void)
{
    int i;

    for (i = 0; i < 10; i++)
        _putchar(i + '0');  /* convierte entero a caracter ASCII */
    _putchar('\n');
}
